name: Test back CI 

on:
  push:
    branches:
      -master
  jobs:
    build:
      runs-on: ubuntu-latest
    steps:
      uses: actions/checkout@v3
      - run: npm install
      - run: npx truffle test
    
